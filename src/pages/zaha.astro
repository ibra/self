---
import Nav from "@components/Nav.astro";
import Layout from "@layouts/BaseLayout.astro";
import Container from "@components/Container.astro";
---

<Layout title="Ibrahim Hisham ~ zaha !1!!" padding="32" path="/creations">
  <Container>
    <Nav />
    <h1
      class="animate-fade antialised font-bold text-2xl my-4 leading-snug mb-0 stagger-50"
    >
      HAPPY ANNIVERSARY ZAHA !!!!
    </h1>
      <p class="animate-fade text-bold stagger-300">
        Happy anniversary to the love of my life and the object of all my desires.
      </p>
      <br />
      <p class="animate-fade stagger-350">
       You're the prettiest loveliest girl there ever was, there is nobody else that i want in any lifetime ever ever ever ever ever. You're the most perfect and the loveliest and you make me better in every single way I know. You make me a better person, you are my better half, and a half i want to stay mine forever.  
      </p>
      <br />
      <p class="animate-fade stagger-400">
       I love you so much and please know I love you infinitely much forever and ever for the rest of my life my heart cut open is just love for you, because you're the best girl there EVER was :]. 
      </p>
    <div>
      <textarea
        id="sleepyZaha"
        class="mb-5"
        rows="8"
        readonly
        style="background-color:#111827; width:250px;resize: none;"
      >
      </textarea>
    </div>
    <h2 class="text-slate-400">meri jaan for</h2>
    <h3 id="daysTogether"></h3>

    <script>
      const confessDate = new Date("2023-05-12T20:58:00");
      const daysTogether = document.getElementById("daysTogether");

      getTimeDifference(confessDate);
      setInterval(() => getTimeDifference(confessDate), 1000);

      let index = 0;
      const catFrames: string[] = [
        `


 
  ／l、             
（-､ - ７         
  l  ~ヽ       
  じしf_,)ノ`,
        `


     z
  ／l、             
（-､ - ７         
  l  ~ヽ       
  じしf_,)ノ`,
        `

      z
     z
  ／l、             
（-､ - ７         
  l  ~ヽ       
  じしf_,)ノ`,
        `
        Z   
      z
     z
  ／l、             
（-､ - ７         
  l  ~ヽ       
  じしf_,)ノ`,
        `
        Z   
      z
     z
  ／l、             
（-､ - ７         
  l  ~ヽ       
  じしf_,)ノ`,
      ];

      sleepyZaha(0);
      setInterval(() => {
        sleepyZaha(index);
        index++;
        if (index == catFrames.length) {
          index = 0;
        }
      }, 400);

      function getTimeDifference(targetDate: Date): void {
        const currentDate = new Date();
        const difference = currentDate.getTime() - targetDate.getTime();

        const days = Math.floor(difference / (1000 * 60 * 60 * 24));
        const hours = Math.floor(
          (difference % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60)
        );
        const minutes = Math.floor(
          (difference % (1000 * 60 * 60)) / (1000 * 60)
        );
        const seconds = Math.floor((difference % (1000 * 60)) / 1000);

        daysTogether!.innerHTML = `${days}d, ${hours}h, ${minutes}m, ${seconds}s`;
      }

      function sleepyZaha(index: number) {
        const textArea = document.getElementById("sleepyZaha") as any;
        textArea!.value = catFrames[index];
      }
    </script>
  </Container>
</Layout>
